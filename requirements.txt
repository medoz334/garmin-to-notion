# Garmin Connect — widget+cffi strategy requires 0.3.2+ (Python 3.10+)
garminconnect>=0.3.2

# Notion
notion-client==2.2.1

# Common
pytz>=2024.1
python-dotenv>=1.0.0

# Other (preserved from original)
withings-sync==4.2.4
lxml>=4.6.0,<5.0
